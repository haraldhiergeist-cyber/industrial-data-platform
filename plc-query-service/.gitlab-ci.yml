
variables:
  IMAGE_NAME: harbor.cluster.lab/industrial/plc-query-service
  IMAGE_TAG: $CI_COMMIT_SHORT_SHA
  INFRA_REPO_URL: "https://oauth2:${INFRA_REPO_PUSH_TOKEN}@gitlab.cluster.lab/root/industrial-data-platform-infra.git"
  INFRA_BRANCH: "main"
  INFRA_APP_PATH: "api/plc-query-service/overlays/dev"

build:
  stage: build
  image: maven:3.9.9-eclipse-temurin-21
  script:
    - mvn -B -DskipTests -pl ./plc-query-service -am clean package
  artifacts:
    paths:
      - target/*.jar
    expire_in: 1 day
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - plc-query-service/**

docker-build:
  stage: docker
  image: docker:27.3.1
  services:
    - name: docker:27.3.1-dind
      command: ["--tls=false"]
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
  script:
    - echo "$HARBOR_PASSWORD" | docker login harbor.cluster.lab -u "$HARBOR_USERNAME" --password-stdin
    - docker build -t ${IMAGE_NAME}:${IMAGE_TAG} -t ${IMAGE_NAME}:latest .
    - docker push ${IMAGE_NAME}:${IMAGE_TAG}
    - docker push ${IMAGE_NAME}:latest
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - plc-query-service/**

update-infra:
  stage: update-infra
  image: alpine:3.20
  before_script:
    - apk add --no-cache git yq
  script:
    - git config --global user.name "gitlab-ci"
    - git config --global user.email "gitlab-ci@cluster.lab"
    - git clone --branch ${INFRA_BRANCH} ${INFRA_REPO_URL} infra
    - cd infra
    - yq -i '(.images[] | select(.name == "harbor.cluster.lab/industrial/plc-query-service").newTag) = strenv(IMAGE_TAG)' ${INFRA_APP_PATH}/kustomization.yaml
    - git add ${INFRA_APP_PATH}/kustomization.yaml
    - |
      if git diff --cached --quiet; then
        echo "No infra changes detected"
      else
        git commit -m "plc-query-service -> ${IMAGE_TAG}"
        git push origin ${INFRA_BRANCH}
      fi
  dependencies: []
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
      changes:
        - plc-query-service/**
